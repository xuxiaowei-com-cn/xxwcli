# This workflow will build a golang project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-go

name: Go

on:
  push:
    branches: [ "main" ]

jobs:

  build:
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write

    strategy:
      matrix:
        os: [ "ubuntu-latest", "windows-latest", "macos-latest" ]

    steps:
      - uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: 1.19

      - name: Cache Go modules
        uses: actions/cache@v3
        with:
          path: ~/go/pkg/mod/cache
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Build
        run: |
          go version
          go mod download
          go build xxwcli.go
          ls

      - name: Set OS variable
        run: echo "::set-output name=os::$(matrix.os/-latest/)"
        id: set_os

      - name: Get current date
        id: date
        run: |
          echo "::set-output name=year_month_day_hour_minute::$(date +'%Y%m%d%H%M')"

      - uses: actions/upload-artifact@v3
        with:
          name: xxwcli-${{ steps.date.outputs.os }}-${{ steps.date.outputs.year_month_day_hour_minute }}
          path: |
            xxwcli
            xxwcli.exe

      - name: Create Release
        uses: ncipollo/release-action@v1
        with:
          artifacts: |
            xxwcli
            xxwcli.exe
          prerelease: true
          name: ${{ steps.date.outputs.os }}-${{ steps.date.outputs.year_month_day_hour_minute }}
          tag: v-${{ steps.date.outputs.os }}-${{ steps.date.outputs.year_month_day_hour_minute }}
          token: ${{ secrets.GITHUB_TOKEN }}
